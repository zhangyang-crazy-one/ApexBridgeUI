# 拖拽功能修复和增强

## 📋 修复概述

修复了拖拽功能的三个关键问题，并添加了节点尺寸调整功能。

**修复日期**: 2025-11-01  
**修复状态**: ✅ 已完成

---

## 🐛 修复的问题

### **问题1：连接线不跟随节点移动** ✅ 已修复

**问题描述**：
- 拖拽物理模型节点时，连接该节点与逻辑模型的垂直连接线保持在原位置
- 连接线没有实时更新，导致视觉效果不佳

**根本原因**：
- `updateConnectionLines()` 函数是占位实现，没有实际更新连接线
- 连接线没有标记，无法快速查找需要更新的连接线

**解决方案**：
1. 在 `drawVerticalRelations()` 中为每条连接线添加 `data-target` 属性标记目标节点ID
2. 添加 `data-source-x` 和 `data-source-y` 属性保存起点坐标
3. 完善 `updateConnectionLines()` 函数，根据节点当前位置实时更新连接线路径
4. 在 `onDrag()` 中调用 `updateConnectionLines()`

**修改代码**：
```javascript
// drawVerticalRelations() - 添加标记
g.append('path')
  .attr('class', 'vertical-relation')
  .attr('data-target', physical.id)
  .attr('data-source-x', x1)
  .attr('data-source-y', y1)
  .attr('d', `M ${x1} ${y1} L ${x2} ${y2}`);

// updateConnectionLines() - 实时更新
function updateConnectionLines() {
  const customPos = customLayout[dragState.currentNodeId] || { x: physical.x, y: physical.y || 0 };
  const nodeWidth = customPos.width || 90;
  const currentX = customPos.x + dragState.offsetX + nodeWidth / 2;
  const currentY = dragState.layerY + 50 + customPos.y + dragState.offsetY;
  
  g.selectAll(`.vertical-relation[data-target="${dragState.currentNodeId}"]`)
    .attr('d', function() {
      const path = d3.select(this);
      const x1 = parseFloat(path.attr('data-source-x'));
      const y1 = parseFloat(path.attr('data-source-y'));
      return `M ${x1} ${y1} L ${currentX} ${currentY}`;
    });
}
```

---

### **问题2：点击节点不显示详情** ✅ 已修复

**问题描述**：
- 点击物理模型节点后，详情面板不显示节点信息
- 拖拽事件可能阻止了点击事件

**根本原因**：
- `mousedown` 事件触发拖拽逻辑，即使只是点击也会进入拖拽状态
- 没有区分点击和拖拽操作

**解决方案**：
1. 在 `endDrag()` 中计算拖拽距离
2. 如果距离 < 5px，认为是点击而非拖拽
3. 点击时触发 `selectModel()` 显示详情
4. 只有真正拖拽时才保存位置和重新渲染

**修改代码**：
```javascript
function endDrag(event) {
  // 检查是否是点击（拖拽距离很小）
  const distance = Math.sqrt(dragState.offsetX ** 2 + dragState.offsetY ** 2);
  const isClick = distance < 5;
  
  if (physical && !isClick) {
    // 只有在真正拖拽时才保存位置
    customLayout[dragState.currentNodeId] = {
      ...customLayout[dragState.currentNodeId],
      x: newX,
      y: newY
    };
    saveLayout();
  }
  
  // 如果是点击，触发选择事件
  if (isClick && physical) {
    selectModel(physical, 'physical');
  } else if (!isClick) {
    renderArchitecture();
  }
}
```

---

### **问题3：节点尺寸不可调整** ✅ 已实现

**问题描述**：
- 物理模型节点的宽度（90px）和高度（60px）是固定的
- 用户无法根据需要调整节点大小

**解决方案**：
1. 在每个节点的右下角添加调整手柄（小圆圈）
2. 实现调整尺寸的拖拽逻辑（`startResize`, `onResize`, `endResize`）
3. 限制最小尺寸（60px × 40px）
4. 更新文字位置以居中显示
5. 保存自定义尺寸到 `customLayout`
6. 调整尺寸时实时更新连接线

**新增功能**：
- ✅ 鼠标悬停节点时显示调整手柄
- ✅ 拖拽手柄调整节点尺寸
- ✅ 调整时文字自动居中
- ✅ 调整时连接线实时更新
- ✅ 自定义尺寸保存到localStorage
- ✅ 重置布局时恢复默认尺寸

---

## ✨ 新增功能

### 1. 调整手柄样式

**CSS样式**：
```css
.resize-handle {
  fill: var(--button-bg);
  stroke: var(--bg-primary);
  stroke-width: 2px;
  cursor: nwse-resize;
  opacity: 0;
  transition: opacity 0.2s;
}

.model-group:hover .resize-handle {
  opacity: 0.8;
}

.resize-handle:hover {
  opacity: 1;
  fill: var(--text-primary);
}

.resize-handle.resizing {
  opacity: 1;
}
```

**视觉效果**：
- 默认隐藏（opacity: 0）
- 鼠标悬停节点时显示（opacity: 0.8）
- 鼠标悬停手柄时高亮（opacity: 1）
- 调整时保持显示

### 2. 调整尺寸状态管理

**数据结构**：
```javascript
const resizeState = {
  isResizing: false,
  currentNode: null,
  currentNodeId: null,
  startX: 0,
  startY: 0,
  startWidth: 0,
  startHeight: 0,
  minWidth: 60,
  minHeight: 40
};
```

### 3. 自定义布局数据结构

**扩展后的数据结构**：
```javascript
customLayout = {
  "customer_crm": {
    x: 30,        // X坐标
    y: 0,         // Y坐标
    width: 100,   // 宽度（新增）
    height: 70    // 高度（新增）
  }
}
```

### 4. 调整尺寸事件流程

```
mousedown (startResize)
    ↓
    - 记录起始位置和尺寸
    - 设置 isResizing = true
    - 添加调整样式
    - 绑定全局 mousemove 和 mouseup 事件
    ↓
mousemove (onResize)
    ↓
    - 计算新尺寸（限制最小值）
    - 更新节点矩形尺寸
    - 更新文字位置（居中）
    - 更新调整手柄位置
    - 实时更新连接线
    ↓
mouseup (endResize)
    ↓
    - 计算最终尺寸
    - 保存到 customLayout
    - 保存到 localStorage
    - 移除调整样式
    - 重新渲染架构图
    - 清除调整状态
    - 解绑全局事件
```

---

## 📝 详细修改清单

### 1. CSS样式修改（第326-360行）

**添加调整手柄样式**：
- `.resize-handle` - 调整手柄基础样式
- `.model-group:hover .resize-handle` - 悬停时显示
- `.resize-handle:hover` - 悬停手柄时高亮
- `.resize-handle.resizing` - 调整中样式

### 2. JavaScript变量添加（第780-806行）

**添加调整尺寸状态**：
```javascript
const resizeState = {
  isResizing: false,
  currentNode: null,
  currentNodeId: null,
  startX: 0,
  startY: 0,
  startWidth: 0,
  startHeight: 0,
  minWidth: 60,
  minHeight: 40
};
```

### 3. drawVerticalRelations函数修改（第1131-1155行）

**添加连接线标记**：
- 应用自定义布局（包括width）
- 添加 `data-target` 属性
- 添加 `data-source-x` 和 `data-source-y` 属性

### 4. drawPhysicalLayer函数修改（第1094-1148行）

**支持自定义尺寸和调整手柄**：
- 读取自定义宽度和高度
- 文字位置根据尺寸居中
- 添加调整手柄（右下角圆圈）
- 绑定调整尺寸事件

### 5. endDrag函数修复（第1429-1477行）

**添加点击检测**：
- 计算拖拽距离
- 距离 < 5px 时认为是点击
- 点击时触发 `selectModel()`
- 只有真正拖拽时才保存和重新渲染

### 6. updateConnectionLines函数完善（第1539-1560行）

**实时更新连接线**：
- 计算节点当前中心位置（考虑width）
- 查找所有连接到当前节点的连接线
- 根据起点和当前终点重新计算路径

### 7. 新增调整尺寸函数（第1562-1676行）

**三个核心函数**：
- `startResize()` - 开始调整尺寸
- `onResize()` - 调整中（实时更新）
- `endResize()` - 结束调整（保存）
- `updateConnectionLinesForResize()` - 调整时更新连接线

### 8. resetLayout函数更新（第1700-1707行）

**更新提示文字**：
- 从"清除所有自定义位置"改为"清除所有自定义位置和尺寸"
- 重置时同时清除位置和尺寸

---

## 🧪 使用方法

### 拖拽节点
1. 将鼠标悬停在物理模型节点上
2. 按住鼠标左键并拖动
3. 连接线实时跟随节点移动
4. 释放鼠标，节点固定在新位置

### 点击节点查看详情
1. 点击物理模型节点（不拖动）
2. 右侧详情面板显示节点信息
3. 包括节点名称、表数量、平台等

### 调整节点尺寸
1. 将鼠标悬停在物理模型节点上
2. 右下角出现调整手柄（小圆圈）
3. 按住手柄并拖动
4. 节点尺寸实时变化，文字自动居中
5. 连接线实时更新
6. 释放鼠标，尺寸固定

### 重置布局
1. 点击顶部的"重置布局"按钮
2. 在确认对话框中点击"确定"
3. 所有节点恢复默认位置和尺寸（90px × 60px）

---

## 📊 性能优化

### 1. 调整尺寸时使用直接DOM操作
```javascript
// ✅ 高性能：直接修改属性
nodeG.select('.model-node')
  .attr('width', newWidth)
  .attr('height', newHeight);

// ❌ 低性能：重新渲染整个图表
renderArchitecture();
```

### 2. 调整结束后重新渲染
```javascript
function endResize() {
  // 保存最终尺寸
  customLayout[nodeId] = { ...customPos, width: newWidth, height: newHeight };
  
  // 重新渲染（应用实际尺寸）
  renderArchitecture();
}
```

### 3. 连接线实时更新优化
```javascript
// 只更新相关的连接线，不是所有连接线
g.selectAll(`.vertical-relation[data-target="${nodeId}"]`)
  .attr('d', ...);
```

---

## ✅ 测试清单

### 功能测试
- [x] 拖拽节点时连接线实时跟随
- [x] 点击节点显示详情面板
- [x] 可以通过拖拽手柄调整节点尺寸
- [x] 调整尺寸时文字自动居中
- [x] 调整尺寸时连接线实时更新
- [x] 自定义尺寸保存到localStorage
- [x] 刷新页面后尺寸保持不变
- [x] 重置布局恢复默认尺寸

### 视觉测试
- [x] 调整手柄默认隐藏
- [x] 鼠标悬停节点时手柄显示
- [x] 拖拽过程流畅，无卡顿
- [x] 连接线更新流畅

### 边界测试
- [x] 节点尺寸不能小于最小值（60px × 40px）
- [x] 点击和拖拽正确区分（距离阈值5px）
- [x] 同时拖拽和调整尺寸不冲突

---

## 🎯 技术亮点

1. **智能点击检测** - 通过距离阈值区分点击和拖拽
2. **实时连接线更新** - 使用data属性快速查找和更新
3. **高性能调整** - 调整时直接修改DOM，结束后重新渲染
4. **最小尺寸限制** - 防止节点过小导致文字溢出
5. **自动文字居中** - 尺寸变化时文字位置自动调整
6. **完整的状态管理** - 位置和尺寸统一保存到customLayout

---

**所有问题已修复，新功能已实现！** 🎉

